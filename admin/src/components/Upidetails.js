import React from 'react'

const Upidetails = () => {
    return (
        <>
            <div className="app">
                {/* <button onClick={handleAddClick}>Add Item</button> */}
                {/* <AddBox isOpen={isAddBoxOpen} onClose={handleAddClose} onSubmit={handleAddSubmit} /> */}
            </div>
        </>
    )
}

export default Upidetails
